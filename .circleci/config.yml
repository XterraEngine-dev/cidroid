version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "A first hello"

  dependencies:
    override:
      - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies
      - echo y | 'com.android.tools.build:gradle:3.1.4'